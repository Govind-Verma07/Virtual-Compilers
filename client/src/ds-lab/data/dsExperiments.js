export const dsExperiments = [
    {
        id: 1,
        title: "Searching and Sorting Techniques",
        description: "Implementation of searching and sorting techniques including Linear search and Bubble sort.",
        difficulty: "Easy",
        objective: "The aim of this experiment is to understand and implement searching and sorting techniques using Data Structures. Searching is used to find an element in a list, and sorting is used to arrange elements in a specific order such as ascending or descending. These techniques are very important for efficient data processing.",
        theory: "### Searching\nSearching is the process of finding a specific element in a data structure.\n\n### Linear Search\nLinear search checks each element one by one. For example, if we have an array [5, 10, 15, 20] and we want to search for 15, we compare 15 with each element starting from the first until it is found.\n\n### Binary Search\nBinary search works only on sorted arrays. It repeatedly divides the search interval in halves to find the element efficiently.\n\n### Sorting\nSorting is the process of arranging elements in a specific order, usually numerical or lexicographical.\n\n### Bubble Sort\nBubble sort is a simple sorting algorithm that compares adjacent elements and swaps them if they are in the wrong order. For example, [5, 3, 8] would compare 5 and 3, swap them to get [3, 5, 8], then compare 5 and 8 and no swap is needed.\n\n### Importance\nSearching and sorting are fundamental operations that improve the efficiency of data access and management in computer systems.\n\n### Real-life Examples\n- Searching for a contact number in a phone book.\n- Sorting student marks to prepare a merit list.",
        algorithm: [
            "Linear Search Algorithm:",
            "Step 1: Start",
            "Step 2: Read number of elements",
            "Step 3: Store elements in array",
            "Step 4: Read element to search",
            "Step 5: Compare element with each array value",
            "Step 6: If found, display position",
            "Step 7: Else display not found",
            "Step 8: Stop",
            "Bubble Sort Algorithm:",
            "Step 1: Start",
            "Step 2: Start from the first element, compare adjacent elements.",
            "Step 3: If the current element is greater than the next element, swap them.",
            "Step 4: Repeat for all elements in the array.",
            "Step 5: Repeat steps 2-4 until the array is fully sorted.",
            "Step 6: Stop"
        ],
        procedure: [
            "Step 1: Open Data Structure Virtual Lab",
            "Step 2: Select Experiment 1",
            "Step 3: Read Aim and Theory",
            "Step 4: Observe Algorithm",
            "Step 5: Write or load default code",
            "Step 6: Select language (C or C++)",
            "Step 7: Click Run button",
            "Step 8: Observe output",
            "Step 9: Verify result"
        ],
        result: "The program was successfully executed and the searching and sorting techniques were implemented correctly. The element was searched in the array and result was displayed.",
        conclusion: "From this experiment, we learned how to implement searching and sorting techniques using arrays. These techniques help in organizing and accessing data efficiently. Searching and sorting are fundamental operations in Data Structures.",
        defaultCodeC: `#include<stdio.h>\n\nint main()\n{\n    int arr[100], n, i, key, found=0;\n\n    printf("Enter number of elements: ");\n    scanf("%d",&n);\n\n    printf("Enter elements:\\n");\n    for(i=0;i<n;i++)\n        scanf("%d",&arr[i]);\n\n    printf("Enter element to search: ");\n    scanf("%d",&key);\n\n    for(i=0;i<n;i++)\n    {\n        if(arr[i]==key)\n        {\n            found=1;\n            printf("Element found at position %d", i+1);\n            break;\n        }\n    }\n\n    if(found==0)\n        printf("Element not found");\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\nusing namespace std;\n\nint main()\n{\n    int arr[100], n, i, key;\n    bool found=false;\n\n    cout<<"Enter number of elements: ";\n    cin>>n;\n\n    cout<<"Enter elements:\\n";\n    for(i=0;i<n;i++)\n        cin>>arr[i];\n\n    cout<<"Enter element to search: ";\n    cin>>key;\n\n    for(i=0;i<n;i++)\n    {\n        if(arr[i]==key)\n        {\n            found=true;\n            cout<<"Element found at position "<<i+1;\n            break;\n        }\n    }\n\n    if(!found)\n        cout<<"Element not found";\n\n    return 0;\n}`
    },
    {
        id: 2,
        title: "List using Array and Linked List",
        description: "Implementation of List data structure using static arrays and dynamic Linked Lists.",
        difficulty: "Medium",
        objective: "The aim of this experiment is to understand and implement list data structures using arrays and linked lists. A list is a collection of elements stored in a sequence. Using arrays and linked lists, we can store, access, and manage data efficiently.",
        theory: "### What is a List?\nA list is a collection of elements stored in order. For example: 10, 20, 30, 40.\n\n### Array List\nIn an array list, elements are stored in continuous memory locations.\n**Example:** `int arr[5] = {10, 20, 30, 40, 50};`\n\n**Advantages:**\n- Fast access to elements using index.\n- Simple to implement.\n\n**Disadvantages:**\n- Fixed size; cannot be changed at runtime.\n\n### Linked List\nA linked list stores elements using nodes. Each node contains data and a pointer to the next node.\n**Example:** 10 → 20 → 30 → NULL\n\n**Advantages:**\n- Dynamic size; grows and shrinks at runtime.\n- Efficient insertion and deletion of elements.\n\n**Disadvantages:**\n- More memory usage (due to pointers).\n- Slower access to elements (must traverse from head).\n\n### Real-life Example\n- **Playlist:** Managing a sequence of songs.\n- **Train coaches:** Adding or removing coaches in a sequence.\n\n### Importance\nLists are fundamental data structures used as the building blocks for more complex structures like stacks, queues, and graphs.",
        algorithm: [
            "Linked List Creation Algorithm:",
            "Step 1: Start",
            "Step 2: Create node structure with data and next pointer",
            "Step 3: Allocate memory for the node (using malloc in C or new in C++)",
            "Step 4: Store data in the node",
            "Step 5: Link nodes using pointers",
            "Step 6: Traverse and display the list",
            "Step 7: Stop"
        ],
        procedure: [
            "Step 1: Open Virtual Data Structure Lab",
            "Step 2: Select Experiment 2",
            "Step 3: Read Aim and Theory",
            "Step 4: Observe Algorithm",
            "Step 5: Load default program",
            "Step 6: Select language (C or C++)",
            "Step 7: Click Run button",
            "Step 8: Observe output",
            "Step 9: Verify result"
        ],
        result: "The program was successfully executed, and the list was created using array and linked list. The elements were stored and displayed correctly.",
        conclusion: "From this experiment, we learned how to implement lists using arrays and linked lists. Linked lists allow dynamic memory allocation and efficient data management. Lists are an important part of Data Structures.",
        defaultCodeC: `#include<stdio.h>\n#include<stdlib.h>\n\nstruct node\n{\n    int data;\n    struct node *next;\n};\n\nint main()\n{\n    struct node *head=NULL, *newnode, *temp;\n    int n, i;\n\n    printf("Enter number of nodes: ");\n    scanf("%d",&n);\n\n    for(i=0;i<n;i++)\n    {\n        newnode=(struct node*)malloc(sizeof(struct node));\n\n        printf("Enter data: ");\n        scanf("%d",&newnode->data);\n\n        newnode->next=NULL;\n\n        if(head==NULL)\n            head=newnode;\n        else\n        {\n            temp=head;\n            while(temp->next!=NULL)\n                temp=temp->next;\n            temp->next=newnode;\n        }\n    }\n\n    temp=head;\n    printf("Linked List: ");\n\n    while(temp!=NULL)\n    {\n        printf("%d ",temp->data);\n        temp=temp->next;\n    }\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\nusing namespace std;\n\nstruct node\n{\n    int data;\n    node *next;\n};\n\nint main()\n{\n    node *head=NULL, *newnode, *temp;\n    int n, i;\n\n    cout<<"Enter number of nodes: ";\n    cin>>n;\n\n    for(i=0;i<n;i++)\n    {\n        newnode=new node;\n\n        cout<<"Enter data: ";\n        cin>>newnode->data;\n\n        newnode->next=NULL;\n\n        if(head==NULL)\n            head=newnode;\n        else\n        {\n            temp=head;\n            while(temp->next!=NULL)\n                temp=temp->next;\n            temp->next=newnode;\n        }\n    }\n\n    temp=head;\n\n    cout<<"Linked List: ";\n\n    while(temp!=NULL)\n    {\n        cout<<temp->data<<" ";\n        temp=temp->next;\n    }\n\n    return 0;\n}`
    },
    {
        id: 3,
        title: "Stack Push and Pop Operations",
        description: "Implementation of Push and Pop operations on a Stack using an array.",
        difficulty: "Easy",
        objective: "The aim of this experiment is to understand and implement stack data structure using push and pop operations. Stack is a linear data structure that follows Last In First Out (LIFO) principle. Push operation is used to insert elements, and pop operation is used to remove elements.",
        theory: "### What is a Stack?\nA stack is a linear data structure that follows the **Last In First Out (LIFO)** principle.\n\n**Example:** Think of a stack of plates. The last plate placed on top is the first one to be removed.\n\n### Stack Operations\n- **Push:** Used to insert an element onto the top of the stack.\n- **Pop:** Used to remove the top element from the stack.\n- **Peek:** Used to view the current top element without removing it.\n\n### Stack using Array\nA stack can be implemented using an array and a variable called `top`. The `top` variable keeps track of the index of the uppermost element.\n- **Initial State:** `top = -1` (Indicating the stack is empty).\n\n### Stack States\n- **Overflow:** Occurs when we try to push an element into a stack that is already full.\n- **Underflow:** Occurs when we try to pop an element from a stack that is empty.\n\n### Importance\nStacks are widely used in:\n- **Expression Evaluation:** Converting and evaluating mathematical expressions.\n- **Undo Operations:** Keeping track of changes in editors.\n- **Function Calls:** Managing memory for recursive and nested function calls.",
        algorithm: [
            "Step 1: Start program",
            "Step 2: Initialize stack with MAX size and top = -1",
            "Step 3: Push Operation:",
            "   a. Check if stack is full (top == MAX-1)",
            "   b. If full, display 'Overflow'",
            "   c. Else increment top and insert element",
            "Step 4: Pop Operation:",
            "   a. Check if stack is empty (top == -1)",
            "   b. If empty, display 'Underflow'",
            "   c. Else remove top element and decrement top",
            "Step 5: Display stack elements and results",
            "Step 6: Stop program"
        ],
        procedure: [
            "Step 1: Open Data Structure Virtual Lab",
            "Step 2: Select Experiment 3",
            "Step 3: Read Aim and Theory",
            "Step 4: Observe Algorithm",
            "Step 5: Load default stack program",
            "Step 6: Select language (C or C++)",
            "Step 7: Click Run button",
            "Step 8: Observe push and pop output",
            "Step 9: Verify result"
        ],
        result: "The program was successfully executed, and stack operations push and pop were implemented correctly. Elements were inserted and removed following LIFO principle.",
        conclusion: "From this experiment, we learned how stack data structure works using push and pop operations. Stack follows Last In First Out principle and is widely used in many applications such as expression evaluation and function calls.",
        defaultCodeC: `#include<stdio.h>\n\n#define MAX 5\n\nint stack[MAX];\nint top = -1;\n\nvoid push(int value)\n{\n    if(top == MAX-1)\n        printf("Stack Overflow\\n");\n    else\n    {\n        top++;\n        stack[top] = value;\n        printf("%d pushed into stack\\n", value);\n    }\n}\n\nvoid pop()\n{\n    if(top == -1)\n        printf("Stack Underflow\\n");\n    else\n    {\n        printf("%d popped from stack\\n", stack[top]);\n        top--;\n    }\n}\n\nvoid display()\n{\n    int i;\n    printf("Stack elements are:\\n");\n    for(i=top; i>=0; i--)\n        printf("%d\\n", stack[i]);\n}\n\nint main()\n{\n    push(10);\n    push(20);\n    push(30);\n\n    display();\n\n    pop();\n\n    display();\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\nusing namespace std;\n\n#define MAX 5\n\nint stack[MAX];\nint top = -1;\n\nvoid push(int value)\n{\n    if(top == MAX-1)\n        cout<<"Stack Overflow\\n";\n    else\n    {\n        top++;\n        stack[top] = value;\n        cout<<value<<" pushed into stack\\n";\n    }\n}\n\nvoid pop()\n{\n    if(top == -1)\n        cout<<"Stack Underflow\\n";\n    else\n    {\n        cout<<stack[top]<<" popped from stack\\n";\n        top--;\n    }\n}\n\nvoid display()\n{\n    cout<<"Stack elements are:\\n";\n\n    for(int i=top;i>=0;i--)\n        cout<<stack[i]<<endl;\n}\n\nint main()\n{\n    push(10);\n    push(20);\n    push(30);\n\n    display();\n\n    pop();\n\n    display();\n\n    return 0;\n}`
    },
    {
        id: 4,
        title: "Polish Notation and Conversion",
        description: "Implementation of Infix to Postfix conversion using Polish Notation principles.",
        difficulty: "Hard",
        objective: "The aim of this experiment is to understand and implement Polish Notation and its conversion using stack data structure. Polish notation is a way of writing expressions without brackets, which makes expression evaluation easier and faster.",
        theory: "### What is an Expression?\nAn expression is a combination of operators (like +, -, *, /) and operands (like A, B, 1, 2).\n**Example:** A + B\n\n### Types of Expressions\n- **Infix:** The operator is between the operands. Example: A + B\n- **Prefix (Polish Notation):** The operator is before the operands. Example: + A B\n- **Postfix (Reverse Polish Notation):** The operator is after the operands. Example: A B +\n\n### Why Polish Notation is Used?\n1. **No Brackets:** Expressions can be written without needing parentheses to define order.\n2. **Easy Evaluation:** Computers can evaluate postfix expressions much faster using a stack.\n\n### Role of Stack\nA stack is used to store operators and maintain their precedence during the conversion process from infix to postfix.\n\n### Real-life Usage\n- **Compiler Design:** Used by compilers to evaluate arithmetic expressions.\n- **Calculators:** Many technical calculators use Reverse Polish Notation (RPN) for calculations.\n\n### Importance\nUnderstanding these conversions is fundamental for learning how computers process logic and mathematical data.",
        algorithm: [
            "Step 1: Start program",
            "Step 2: Initialize an empty stack for operators",
            "Step 3: Read the infix expression from left to right",
            "Step 4: If the character is an operand, add it to the postfix expression",
            "Step 5: If the character is '(', push it to stack",
            "Step 6: If the character is ')', pop from stack until '(' is found",
            "Step 7: If the character is an operator, pop operators with higher or equal priority from stack to postfix, then push current operator",
            "Step 8: Pop any remaining operators from stack to postfix",
            "Step 9: Display the resulting postfix expression",
            "Step 10: Stop program"
        ],
        procedure: [
            "Step 1: Open Data Structure Virtual Lab",
            "Step 2: Select Experiment 4",
            "Step 3: Read Aim and Theory",
            "Step 4: Observe Algorithm",
            "Step 5: Load default conversion program",
            "Step 6: Select language (C or C++)",
            "Step 7: Click Run button",
            "Step 8: Observe postfix output",
            "Step 9: Verify result"
        ],
        result: "The program was successfully executed, and the infix expression was converted into postfix expression using stack data structure.",
        conclusion: "From this experiment, we learned how Polish notation works and how to convert infix expressions into postfix using stack. This method is useful in expression evaluation and compiler design.",
        defaultCodeC: `#include<stdio.h>\n#include<ctype.h>\n\n#define MAX 100\n\nchar stack[MAX];\nint top = -1;\n\nvoid push(char x)\n{\n    stack[++top] = x;\n}\n\nchar pop()\n{\n    return stack[top--];\n}\n\nint priority(char x)\n{\n    if(x=='(')\n        return 0;\n    if(x=='+' || x=='-')\n        return 1;\n    if(x=='*' || x=='/')\n        return 2;\n    return 0;\n}\n\nint main()\n{\n    char exp[100];\n    char *e, x;\n\n    printf("Enter infix expression: ");\n    scanf("%s", exp);\n\n    e = exp;\n\n    while(*e != '\\0')\n    {\n        if(isalnum(*e))\n            printf("%c", *e);\n\n        else if(*e == '(')\n            push(*e);\n\n        else if(*e == ')')\n        {\n            while((x = pop()) != '(')\n                printf("%c", x);\n        }\n\n        else\n        {\n            while(priority(stack[top]) >= priority(*e))\n                printf("%c", pop());\n            push(*e);\n        }\n\n        e++;\n    }\n\n    while(top != -1)\n        printf("%c", pop());\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\n#include<cctype>\nusing namespace std;\n\n#define MAX 100\n\nchar stack[MAX];\nint top = -1;\n\nvoid push(char x)\n{\n    stack[++top] = x;\n}\n\nchar pop()\n{\n    return stack[top--];\n}\n\nint priority(char x)\n{\n    if(x=='(')\n        return 0;\n    if(x=='+' || x=='-')\n        return 1;\n    if(x=='*' || x=='/')\n        return 2;\n    return 0;\n}\n\nint main()\n{\n    char exp[100];\n    char *e, x;\n\n    cout<<"Enter infix expression: ";\n    cin>>exp;\n\n    e = exp;\n\n    while(*e != '\\0')\n    {\n        if(isalnum(*e))\n            cout<<*e;\n\n        else if(*e == '(')\n            push(*e);\n\n        else if(*e == ')')\n        {\n            while((x = pop()) != '(')\n                cout<<x;\n        }\n\n        else\n        {\n            while(priority(stack[top]) >= priority(*e))\n                cout<<pop();\n            push(*e);\n        }\n\n        e++;\n    }\n\n    while(top != -1)\n        cout<<pop();\n\n    return 0;\n}`
    },
    {
        id: 5,
        title: "Program to Solve Problems using Iteration and Recursion",
        description: "Comparing iterative and recursive approaches for solving problems like Factorial.",
        difficulty: "Easy",
        objective: "The aim of this experiment is to understand and implement problem-solving using iteration and recursion techniques. Iteration uses loops to repeat instructions, while recursion uses function calls to solve problems step by step. These techniques are important in Data Structures and programming.",
        theory: "### Iteration\nIteration is the process of repeating instructions using loops until a specific condition is met.\n- **Common Loops:** `for` loop, `while` loop.\n- **Example:** Calculating Factorial by multiplying numbers 1 to n in a loop.\n\n### Recursion\nRecursion is a technique where a function calls itself to solve a smaller version of the same problem.\n- **Example:** Calculating Factorial using the formula `n! = n * (n-1)!`.\n\n### How Recursion Works\nEach recursive call reduces the size of the problem. This continue until a base case is reached.\n\n### Base Condition\nThe base condition is a state where the problem is small enough to be solved directly without further recursion. It is critical because it stops the recursive process and prevents infinite loops (stack overflow).\n\n### Iteration vs Recursion\n- **Iteration:** Uses loops (`for`, `while`). Generally more memory-efficient.\n- **Recursion:** Uses function calls. Often results in simpler and cleaner code for complex problems like tree traversals.\n\n### Real-life Examples\n- **Factorial Calculation:** Basic math operations.\n- **Tree Traversal:** Navigating folder structures on a computer.",
        algorithm: [
            "Step 1: Start program",
            "Step 2: Read number from the user",
            "Step 3: Calculate factorial using iteration (using a for loop)",
            "Step 4: Calculate factorial using recursion (function calling itself)",
            "Step 5: Display results of both methods",
            "Step 6: Stop program"
        ],
        procedure: [
            "Step 1: Open Data Structure Virtual Lab",
            "Step 2: Select Experiment 5",
            "Step 3: Read Aim and Theory",
            "Step 4: Observe Algorithm",
            "Step 5: Load default program",
            "Step 6: Select language (C or C++)",
            "Step 7: Click Run button",
            "Step 8: Observe factorial output",
            "Step 9: Verify result"
        ],
        result: "The program was successfully executed, and factorial was calculated using both iteration and recursion methods.",
        conclusion: "From this experiment, we learned how to solve problems using iteration and recursion. Iteration uses loops, while recursion uses function calls. Both techniques are important in Data Structures and programming.",
        defaultCodeC: `#include<stdio.h>\n\nint factorialRec(int n)\n{\n    if(n==0)\n        return 1;\n    else\n        return n * factorialRec(n-1);\n}\n\nint main()\n{\n    int n, i, fact=1;\n\n    printf("Enter number: ");\n    scanf("%d",&n);\n\n    for(i=1;i<=n;i++)\n        fact = fact * i;\n\n    printf("Factorial using iteration = %d\\n", fact);\n\n    printf("Factorial using recursion = %d\\n", factorialRec(n));\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\nusing namespace std;\n\nint factorialRec(int n)\n{\n    if(n==0)\n        return 1;\n    else\n        return n * factorialRec(n-1);\n}\n\nint main()\n{\n    int n, i, fact=1;\n\n    cout<<"Enter number: ";\n    cin>>n;\n\n    for(i=1;i<=n;i++)\n        fact = fact * i;\n\n    cout<<"Factorial using iteration = "<<fact<<endl;\n\n    cout<<"Factorial using recursion = "<<factorialRec(n)<<endl;\n\n    return 0;\n}`
    },
    {
        id: 6,
        title: "Program for Recursion Removal using Stack",
        description: "Simulating recursion by manually managing a Stack structure.",
        difficulty: "Hard",
        objective: "The aim of this experiment is to understand how recursion can be replaced using stack data structure. Recursion uses function calls, which internally use stack memory. This experiment helps students implement recursion removal manually using stack operations.",
        theory: "### What is Recursion?\nRecursion is a method where a function calls itself to solve a problem.\n**Example:** Calculating Factorial using `fact(n) = n * fact(n-1)`.\n\n### Problem with Recursion\nWhile recursion produces clean code, it uses the system's call stack memory. If millions of recursive calls are made, it can lead to a **Stack Overflow** error.\n\n### Recursion Removal\nWe can remove recursion by implementing our own stack. This simulates the behavior of the computer's internal stack but gives the programmer more control over memory.\n\n### How Stack Helps\nA stack data structure stores the intermediate values that a recursive function would normally keep in memory.\n\n### Steps for Removal\n1. Use a loop instead of self-calling functions.\n2. Push the task's parameters onto a manual stack.\n3. Pop the values and process them until the stack is empty.\n\n### Real-life Usage\n- **Compiler Design:** Compilers convert recursive code into iterative code for better performance.\n- **Expression Evaluation:** Parsing complex nested math expressions.\n- **Function Call Simulation:** Understanding how operating systems handle task switching.\n\n### Importance\nLearning recursion removal helps students understand the deep relationship between recursion and stacks, which is a core concept in advanced Data Structures.",
        algorithm: [
            "Step 1: Start program",
            "Step 2: Input the number 'n' to calculate factorial",
            "Step 3: Push values from 1 to n into a manual stack",
            "Step 4: Pop values from the stack one by one",
            "Step 5: Multiply the popped values to get the result",
            "Step 6: Display the factorial result",
            "Step 7: Stop program"
        ],
        procedure: [
            "Step 1: Open Data Structure Virtual Lab",
            "Step 2: Select Experiment 6",
            "Step 3: Read Aim and Theory",
            "Step 4: Observe Algorithm",
            "Step 5: Load default stack program",
            "Step 6: Select language (C or C++)",
            "Step 7: Click Run button",
            "Step 8: Observe factorial output",
            "Step 9: Verify result"
        ],
        result: "The program was successfully executed, and recursion was removed using stack data structure. The factorial was calculated correctly using stack operations.",
        conclusion: "From this experiment, we learned how recursion can be replaced using stack data structure. Stack helps simulate recursive function calls manually and improves understanding of recursion.",
        defaultCodeC: `#include<stdio.h>\n\n#define MAX 100\n\nint stack[MAX];\nint top = -1;\n\nvoid push(int value)\n{\n    stack[++top] = value;\n}\n\nint pop()\n{\n    return stack[top--];\n}\n\nint main()\n{\n    int n, i, result = 1;\n\n    printf("Enter number: ");\n    scanf("%d", &n);\n\n    for(i=1;i<=n;i++)\n        push(i);\n\n    while(top != -1)\n        result = result * pop();\n\n    printf("Factorial using stack = %d", result);\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\nusing namespace std;\n\n#define MAX 100\n\nint stack[MAX];\nint top = -1;\n\nvoid push(int value)\n{\n    stack[++top] = value;\n}\n\nint pop()\n{\n    return stack[top--];\n}\n\nint main()\n{\n    int n, i, result = 1;\n\n    cout<<"Enter number: ";\n    cin>>n;\n\n    for(i=1;i<=n;i++)\n        push(i);\n\n    while(top != -1)\n        result = result * pop();\n\n    cout<<"Factorial using stack = "<<result;\n\n    return 0;\n}`
    },
    {
        id: 7,
        title: "Queue and Priority Queue Implementation",
        description: "Implementing standard Queues and Priority Queues for process scheduling.",
        difficulty: "Medium",
        objective: "The aim of this experiment is to understand and implement queue and priority queue data structures. Queue follows First In First Out (FIFO) principle, where the first element inserted is the first to be removed. Priority queue removes elements based on priority instead of insertion order.",
        theory: "### What is a Queue?\nA queue is a linear data structure that follows the **First In First Out (FIFO)** principle.\n**Example:** Think of a queue of people in line. The first person to enter the line is the first person to exit.\n\n### Queue Operations\n- **Enqueue:** Used to insert an element at the back (rear) of the queue.\n- **Dequeue:** Used to remove the front element from the queue.\n- **Front:** Refers to the first element in the queue.\n\n### Queue using Array\nA queue can be implemented using an array and two variables: `front` and `rear`.\n- **Initial State:** `front = -1`, `rear = -1` (Indicating the queue is empty).\n\n### Queue States\n- **Overflow:** Occurs when we try to insert an element into a queue that is already full.\n- **Underflow:** Occurs when we try to remove an element from an empty queue.\n\n### Priority Queue\nA priority queue is a special type of queue where each element has an associated priority. Elements with higher priority are dequeued before elements with lower priority, regardless of their insertion order.\n**Example:** Process scheduling in an Operating System, where urgent tasks are processed before normal tasks.\n\n### Importance\nQueues and priority queues are used in:\n- **CPU Scheduling:** Managing tasks in an operating system.\n- **Printer Queue:** Managing documents waiting to be printed.\n- **Task Management:** Real-time data processing and network routing.",
        algorithm: [
            "Step 1: Start program",
            "Step 2: Initialize queue with MAX size and front = -1, rear = -1",
            "Step 3: Enqueue Operation:",
            "   a. Check if rear == MAX-1 (Overflow)",
            "   b. If front == -1, set front = 0",
            "   c. Increment rear and insert element",
            "Step 4: Dequeue Operation:",
            "   a. Check if front == -1 or front > rear (Underflow)",
            "   b. Else remove element at front and increment front",
            "Step 5: Display queue elements and results",
            "Step 6: Stop program"
        ],
        procedure: [
            "Step 1: Open Data Structure Virtual Lab",
            "Step 2: Select Experiment 7",
            "Step 3: Read Aim and Theory",
            "Step 4: Observe Algorithm",
            "Step 5: Load default queue program",
            "Step 6: Select language (C or C++)",
            "Step 7: Click Run button",
            "Step 8: Observe queue output",
            "Step 9: Verify result"
        ],
        result: "The program was successfully executed, and queue operations insertion and deletion were implemented correctly. Elements were processed in FIFO order.",
        conclusion: "From this experiment, we learned how queue and priority queue data structures work. Queue follows FIFO principle, and priority queue removes elements based on priority. These are useful in many real-world applications.",
        defaultCodeC: `#include<stdio.h>\n\n#define MAX 5\n\nint queue[MAX];\nint front = -1, rear = -1;\n\nvoid enqueue(int value)\n{\n    if(rear == MAX-1)\n        printf("Queue Overflow\\n");\n    else\n    {\n        if(front == -1)\n            front = 0;\n        queue[++rear] = value;\n        printf("%d inserted into queue\\n", value);\n    }\n}\n\nvoid dequeue()\n{\n    if(front == -1 || front > rear)\n        printf("Queue Underflow\\n");\n    else\n        printf("%d removed from queue\\n", queue[front++]);\n}\n\nvoid display()\n{\n    int i;\n\n    printf("Queue elements:\\n");\n\n    for(i=front;i<=rear;i++)\n        printf("%d\\n", queue[i]);\n}\n\nint main()\n{\n    enqueue(10);\n    enqueue(20);\n    enqueue(30);\n\n    display();\n\n    dequeue();\n\n    display();\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\nusing namespace std;\n\n#define MAX 5\n\nint queue[MAX];\nint front = -1, rear = -1;\n\nvoid enqueue(int value)\n{\n    if(rear == MAX-1)\n        cout<<"Queue Overflow\\n";\n    else\n    {\n        if(front == -1)\n            front = 0;\n\n        queue[++rear] = value;\n\n        cout<<value<<" inserted into queue\\n";\n    }\n}\n\nvoid dequeue()\n{\n    if(front == -1 || front > rear)\n        cout<<"Queue Underflow\\n";\n    else\n        cout<<queue[front++]<<" removed from queue\\n";\n}\n\nvoid display()\n{\n    cout<<"Queue elements:\\n";\n\n    for(int i=front;i<=rear;i++)\n        cout<<queue[i]<<endl;\n}\n\nint main()\n{\n    enqueue(10);\n    enqueue(20);\n    enqueue(30);\n\n    display();\n\n    dequeue();\n\n    display();\n\n    return 0;\n}`
    },
    {
        id: 8,
        title: "Tree Structure and Traversal Techniques",
        description: "Implementing a Binary Tree and performing Inorder, Preorder, and Postorder traversals.",
        difficulty: "Medium",
        objective: "The aim of this experiment is to understand and implement tree data structure and perform different traversal techniques such as inorder, preorder, and postorder. Tree is a non-linear data structure used to store hierarchical data.",
        theory: "### What is a Tree?\nA tree is a hierarchical, non-linear data structure that consists of nodes connected by edges.\n\n### Terminology\n- **Root:** The topmost node of the tree.\n- **Child:** A node connected to another node when moving away from the root.\n- **Parent:** The node above a child node.\n- **Leaf:** A node with no children.\n\n### Binary Tree\nA binary tree is a type of tree where each node can have a maximum of two children, typically called the **Left Child** and the **Right Child**.\n\n### Tree Traversal\nTraversal is the process of visiting every node in the tree exactly once. There are three common ways to traverse a binary tree:\n1. **Inorder:** Left Subtree → Root → Right Subtree\n2. **Preorder:** Root → Left Subtree → Right Subtree\n3. **Postorder:** Left Subtree → Right Subtree → Root\n\n### Real-life Examples\n- **File System:** Folders and files are organized in a tree structure.\n- **Family Tree:** Showing relationships and hierarchy between generations.\n\n### Importance\nTrees are used to represent objects that have hierarchical relationships and are essential for efficient data searching and sorting operations.",
        algorithm: [
            "Step 1: Start program",
            "Step 2: Create a tree structure using a node with data, left pointer, and right pointer",
            "Step 3: Insert nodes into the tree",
            "Step 4: Perform Inorder traversal (Left, Root, Right)",
            "Step 5: Perform Preorder traversal (Root, Left, Right)",
            "Step 6: Perform Postorder traversal (Left, Right, Root)",
            "Step 7: Display the results of each traversal",
            "Step 8: Stop program"
        ],
        procedure: [
            "Step 1: Open Data Structure Virtual Lab",
            "Step 2: Select Experiment 8",
            "Step 3: Read Aim and Theory",
            "Step 4: Observe Algorithm",
            "Step 5: Load default tree program",
            "Step 6: Select language (C or C++)",
            "Step 7: Click Run button",
            "Step 8: Observe traversal output",
            "Step 9: Verify result"
        ],
        result: "The program was successfully executed, and tree traversal techniques inorder, preorder, and postorder were implemented correctly.",
        conclusion: "From this experiment, we learned how tree data structure works and how to perform traversal techniques. Tree is useful for storing hierarchical data.",
        defaultCodeC: `#include<stdio.h>\n#include<stdlib.h>\n\nstruct node\n{\n    int data;\n    struct node *left, *right;\n};\n\nstruct node* createNode(int value)\n{\n    struct node* newNode = (struct node*)malloc(sizeof(struct node));\n\n    newNode->data = value;\n    newNode->left = newNode->right = NULL;\n\n    return newNode;\n}\n\nvoid inorder(struct node* root)\n{\n    if(root != NULL)\n    {\n        inorder(root->left);\n        printf("%d ", root->data);\n        inorder(root->right);\n    }\n}\n\nvoid preorder(struct node* root)\n{\n    if(root != NULL)\n    {\n        printf("%d ", root->data);\n        preorder(root->left);\n        preorder(root->right);\n    }\n}\n\nvoid postorder(struct node* root)\n{\n    if(root != NULL)\n    {\n        postorder(root->left);\n        postorder(root->right);\n        printf("%d ", root->data);\n    }\n}\n\nint main()\n{\n    struct node* root = createNode(10);\n\n    root->left = createNode(20);\n    root->right = createNode(30);\n\n    root->left->left = createNode(40);\n    root->left->right = createNode(50);\n\n    printf("Inorder Traversal: ");\n    inorder(root);\n\n    printf("\\nPreorder Traversal: ");\n    preorder(root);\n\n    printf("\\nPostorder Traversal: ");\n    postorder(root);\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\nusing namespace std;\n\nstruct node\n{\n    int data;\n    node *left, *right;\n};\n\nnode* createNode(int value)\n{\n    node* newNode = new node();\n\n    newNode->data = value;\n    newNode->left = newNode->right = NULL;\n\n    return newNode;\n}\n\nvoid inorder(node* root)\n{\n    if(root != NULL)\n    {\n        inorder(root->left);\n        cout<<root->data<<" ";\n        inorder(root->right);\n    }\n}\n\nvoid preorder(node* root)\n{\n    if(root != NULL)\n    {\n        cout<<root->data<<" ";\n        preorder(root->left);\n        preorder(root->right);\n    }\n}\n\nvoid postorder(node* root)\n{\n    if(root != NULL)\n    {\n        postorder(root->left);\n        postorder(root->right);\n        cout<<root->data<<" ";\n    }\n}\n\nint main()\n{\n    node* root = createNode(10);\n\n    root->left = createNode(20);\n    root->right = createNode(30);\n\n    root->left->left = createNode(40);\n    root->left->right = createNode(50);\n\n    cout<<"Inorder Traversal: ";\n    inorder(root);\n\n    cout<<"\\nPreorder Traversal: ";\n    preorder(root);\n\n    cout<<"\\nPostorder Traversal: ";\n    postorder(root);\n\n    return 0;\n}`
    },
    {
        id: 9,
        title: "Graph Structure and Traversal Techniques (DFS and BFS)",
        description: "Implementation of Graph representation and traversal using BFS and DFS.",
        difficulty: "Hard",
        objective: "The aim of this experiment is to understand and implement graph data structure and perform traversal techniques such as Depth First Search (DFS) and Breadth First Search (BFS). Graph is an important non-linear data structure used to represent networks such as social networks, computer networks, and maps.",
        theory: "### What is a Graph?\nA graph is a non-linear data structure consisting of a finite set of vertices (or nodes) and a set of edges that connect a pair of nodes.\n**Example:** Think of cities connected by roads. Cities are vertices, and the roads are edges.\n\n### Terminology\n- **Vertex:** A node in the graph.\n- **Edge:** A connection between two nodes.\n- **Adjacent Vertices:** Two nodes are adjacent if they are connected by a common edge.\n\n### Graph Representation\nGraphs can be represented in two main ways:\n1. **Adjacency Matrix:** A 2D array where `matrix[i][j] = 1` if there is an edge between node `i` and `j`.\n2. **Adjacency List:** An array of lists where each list describes the neighbors of a vertex.\n\n### Graph Traversal\nTraversal means visiting all the vertices of a graph in a specific order. The two most common methods are:\n\n### 1. Depth First Search (DFS)\nDFS starts at a starting node and explores as far as possible along each branch before backtracking.\n- **Logic:** It visits nodes 'deeply' before visiting neighbors.\n- **Data Structure:** Uses a **Stack** (often implemented using recursion).\n\n### 2. Breadth First Search (BFS)\nBFS starts at a starting node and explores all neighbor nodes at the present depth before moving on to nodes at the next depth level.\n- **Logic:** It visits nodes 'level by level'.\n- **Data Structure:** Uses a **Queue**.\n\n### Real-life Examples\n- **Google Maps:** Finding the shortest path between two locations.\n- **Social Networks:** Suggesting friends based on mutual connections.\n- **Computer Networks:** Routing data packets between servers.\n\n### Importance\nGraph algorithms are fundamental for path finding, network analysis, and artificial intelligence applications.",
        algorithm: [
            "Step 1: Start program",
            "Step 2: Create a graph using an adjacency matrix",
            "Step 3: DFS Algorithm: Mark node visited, visit its unvisited neighbors recursively",
            "Step 4: BFS Algorithm: Use a queue to visit nodes level by level, marking them visited",
            "Step 5: Display the results of each traversal technique",
            "Step 6: Stop program"
        ],
        procedure: [
            "Step 1: Open Virtual Programming Lab",
            "Step 2: Select Data Structure Lab",
            "Step 3: Select Experiment 9",
            "Step 4: Read Aim and Theory",
            "Step 5: Observe Algorithm",
            "Step 6: Load default graph program",
            "Step 7: Select language (C or C++)",
            "Step 8: Click Run button",
            "Step 9: Observe DFS and BFS output",
            "Step 10: Verify result"
        ],
        result: "The program was successfully executed, and graph traversal techniques DFS and BFS were implemented correctly. All graph vertices were visited using traversal methods.",
        conclusion: "From this experiment, we learned how graph data structure works and how to perform DFS and BFS traversal techniques. Graph traversal is important for network analysis and path finding.",
        defaultCodeC: `#include<stdio.h>\n\n#define MAX 10\n\nint graph[MAX][MAX], visited[MAX], n;\n\nvoid DFS(int node)\n{\n    int i;\n\n    printf("%d ", node);\n\n    visited[node] = 1;\n\n    for(i=0;i<n;i++)\n    {\n        if(graph[node][i] == 1 && visited[i] == 0)\n        {\n            DFS(i);\n        }\n    }\n}\n\nint main()\n{\n    int i, j;\n\n    printf("Enter number of vertices: ");\n    scanf("%d", &n);\n\n    printf("Enter adjacency matrix:\\n");\n\n    for(i=0;i<n;i++)\n        for(j=0;j<n;j++)\n            scanf("%d", &graph[i][j]);\n\n    for(i=0;i<n;i++)\n        visited[i] = 0;\n\n    printf("DFS Traversal: ");\n\n    DFS(0);\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\nusing namespace std;\n\n#define MAX 10\n\nint graph[MAX][MAX], visited[MAX], n;\n\nvoid DFS(int node)\n{\n    cout<<node<<" ";\n\n    visited[node] = 1;\n\n    for(int i=0;i<n;i++)\n    {\n        if(graph[node][i] == 1 && visited[i] == 0)\n        {\n            DFS(i);\n        }\n    }\n}\n\nint main()\n{\n    cout<<"Enter number of vertices: ";\n    cin>>n;\n\n    cout<<"Enter adjacency matrix:\\n";\n\n    for(int i=0;i<n;i++)\n        for(int j=0;j<n;j++)\n            cin>>graph[i][j];\n\n    for(int i=0;i<n;i++)\n        visited[i] = 0;\n\n    cout<<"DFS Traversal: ";\n\n    DFS(0);\n\n    return 0;\n}`
    },
    {
        id: 10,
        title: "Shortest Path in Graph using Dijkstra’s Algorithm",
        description: "Implementing Dijkstra's algorithm to find the shortest path between nodes.",
        difficulty: "Hard",
        objective: "The aim of this experiment is to understand and implement Dijkstra’s Algorithm to find the shortest path between nodes in a graph. This algorithm helps find the minimum distance from a source vertex to all other vertices in a weighted graph.",
        theory: "### What is a Graph?\nA graph is a non-linear data structure consisting of vertices (nodes) and edges (connections between nodes).\n\n### Weighted Graph\nA weighted graph is a graph where each edge has a 'weight' or 'cost' assigned to it. This weight could represent distance, time, or cost.\n**Example:** Distance in kilometers between two cities.\n\n### Shortest Path Problem\nThe shortest path problem is about finding the path between two vertices such that the sum of the weights of its constituent edges is minimized.\n\n### Dijkstra’s Algorithm\nDijkstra’s algorithm is a popular algorithm used to find the shortest path from a single source vertex to all other vertices in a weighted graph.\n\n### How it Works\n1. **Initialization:** Assign a distance of 0 to the source node and infinity (INF) to all other nodes.\n2. **Min Distance Selection:** Select the unvisited node with the smallest distance.\n3. **Update Neighbors:** For each neighbor of the current node, calculate its distance from the source through the current node. If this new distance is smaller than the previously recorded distance, update it.\n4. **Repeat:** Mark the current node as visited and repeat the process for all nodes.\n\n### Real-life Examples\n- **Google Maps:** Calculating the fastest route between two locations.\n- **Network Routing:** Finding the most efficient path for data packets in a computer network.\n- **GPS Navigation:** Guiding drivers to their destination using the shortest distance.\n\n### Importance\nDijkstra's algorithm is a fundamental algorithm in computer science, essential for solving optimization problems in transportation, networking, and geography.",
        algorithm: [
            "Step 1: Start program",
            "Step 2: Input the number of vertices and the weighted adjacency matrix",
            "Step 3: Initialize a distance array with infinity (INF) and a visited array with 0",
            "Step 4: Assign distance 0 to the source vertex",
            "Step 5: Find the unvisited vertex with the minimum distance",
            "Step 6: Update the distance of all adjacent vertices of the selected vertex",
            "Step 7: Repeat the process until all vertices are processed",
            "Step 8: Display the shortest distance from the source to each vertex",
            "Step 9: Stop program"
        ],
        procedure: [
            "Step 1: Open Virtual Programming Lab",
            "Step 2: Select Data Structure Lab",
            "Step 3: Select Experiment 10",
            "Step 4: Read Aim and Theory",
            "Step 5: Observe Algorithm",
            "Step 6: Load default Dijkstra program",
            "Step 7: Select language (C or C++)",
            "Step 8: Click Run button",
            "Step 9: Observe shortest path output",
            "Step 10: Verify result"
        ],
        result: "The program was successfully executed, and shortest path between graph vertices was calculated using Dijkstra’s algorithm.",
        conclusion: "From this experiment, we learned how to find shortest path in a graph using Dijkstra’s algorithm. This algorithm is useful in navigation systems and network routing.",
        defaultCodeC: `#include<stdio.h>\n\n#define MAX 10\n#define INF 9999\n\nint main()\n{\n    int graph[MAX][MAX], distance[MAX], visited[MAX];\n    int n, i, j, count, min, next;\n\n    printf("Enter number of vertices: ");\n    scanf("%d", &n);\n\n    printf("Enter adjacency matrix:\\n");\n\n    for(i=0;i<n;i++)\n        for(j=0;j<n;j++)\n            scanf("%d", &graph[i][j]);\n\n    for(i=0;i<n;i++)\n    {\n        distance[i] = INF;\n        visited[i] = 0;\n    }\n\n    distance[0] = 0;\n\n    for(count=0;count<n-1;count++)\n    {\n        min = INF;\n\n        for(i=0;i<n;i++)\n        {\n            if(distance[i] < min && !visited[i])\n            {\n                min = distance[i];\n                next = i;\n            }\n        }\n\n        visited[next] = 1;\n\n        for(i=0;i<n;i++)\n        {\n            if(!visited[i] && graph[next][i] &&\n               distance[next] + graph[next][i] < distance[i])\n            {\n                distance[i] = distance[next] + graph[next][i];\n            }\n        }\n    }\n\n    printf("Shortest distances from source:\\n");\n\n    for(i=0;i<n;i++)\n        printf("%d -> %d = %d\\n", 0, i, distance[i]);\n\n    return 0;\n}`,
        defaultCodeCPP: `#include<iostream>\nusing namespace std;\n\n#define MAX 10\n#define INF 9999\n\nint main()\n{\n    int graph[MAX][MAX], distance[MAX], visited[MAX];\n    int n, i, j, count, min, next;\n\n    cout<<"Enter number of vertices: ";\n    cin>>n;\n\n    cout<<"Enter adjacency matrix:\\n";\n\n    for(i=0;i<n;i++)\n        for(j=0;j<n;j++)\n            cin>>graph[i][j];\n\n    for(i=0;i<n;i++)\n    {\n        distance[i] = INF;\n        visited[i] = 0;\n    }\n\n    distance[0] = 0;\n\n    for(count=0;count<n-1;count++)\n    {\n        min = INF;\n\n        for(i=0;i<n;i++)\n        {\n            if(distance[i] < min && !visited[i])\n            {\n                min = distance[i];\n                next = i;\n            }\n        }\n\n        visited[next] = 1;\n\n        for(i=0;i<n;i++)\n        {\n            if(!visited[i] && graph[next][i] &&\n               distance[next] + graph[next][i] < distance[i])\n            {\n                distance[i] = distance[next] + graph[next][i];\n            }\n        }\n    }\n\n    cout<<"Shortest distances from source:\\n";\n\n    for(i=0;i<n;i++)\n        cout<<"0 -> "<<i<<" = "<<distance[i]<<endl;\n\n    return 0;\n}`
    }
];
